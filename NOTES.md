# What this platform does that surprises you
Generated by `notes_index.py` from the places seven demos wrote things down, plus the pooled professional-services corpus in `learnings/`. Do not edit by hand: add the note to its source and re-run.
| | demo series | pooled PS | total |
|---|---|---|---|
| recorded learnings | 73 | 151 | 224 |
| enforced by a check | 29 | 49 | 78 |
| convention only, cannot be checked | 16 | 25 | 41 |
| not yet triaged | 28 | 77 | 105 |

Pooled entries come from DM, NC, NR, PS (`learnings/`), each produced independently with the prompt in `EXTRACT-LEARNINGS.md`. The id in each row is the entry, where the evidence and its provenance live.

## Enforced

| Learning | Layer | Check |
|---|---|---|
| `return` inside a block does not exit the procedure | `lint` | `indented-return` |
| Reserved words: `update` and `rule` | `lint` | `reserved-var` |
| `update` is a reserved word in VAIL | `lint` | `reserved-var` |
| Procedures carry their own `vailErrors`, and the service does not aggregate them | `client` | `vail_errors, both levels` |
| VAIL has no `while` loop | `lint` | `while-loop` |
| `DELETE` requires a WHERE clause | `lint` | `delete-no-where` |
| `date()` compiles and returns a String | `lint` | `date-call` |
| `ars_dontExport` persists, so it has to be cleared as well as set | `publish` | `export policy follows the credential` |
| `system.scheduledevents`: required fields, and `interval` is milliseconds | `opscheck` | `required fields, interval in ms` |
| 18. `sort()` takes a field NAME, not a closure | `lint` | `sort-closure` |
| 19. A map is a Groovy `LinkedHashMap`: `keySet()`, not `keys()` | `lint` | `map-keys` |
| 20. The in-block `return` trap has a second edge | `lint` | `indented-return` |
| 23. `overflow-x: auto` does not let a grid item shrink | `uilint` | `grid-min-width` |
| 26. A clean push does NOT mean the service compiles | `push` | `smoke` |
| 30. An idle browser tab was generating ~130,000 procedure calls a day | `opscheck` | `idle cost per day` |
| 31. An O(n^3) scan on a POLLED procedure | `opscheck` | `polled procedure cost` |
| 32. Every entity publish wakes all four monitoring handlers | `opscheck` | `handler fan-out` |
| 33. The ops hump is namespace LOAD, not namespace activity | `opscheck` | `load, not activity` |
| 35. Every namespace runs schedulers that ignore whether the demo is running | `opscheck` | `schedulers while stopped` |
| 40. A scheduled procedure fires once per cluster node, so note 35 was 3x low | `opscheck` | `per-node multiplication` |
| 42. A standalone procedure hides from a filter written for service procedures | `client` | `procedures() filters on serviceName` |
| 43. Two ways an export lies, both found by making it prove itself | `publish` | `verify by read-back` |
| 44. `vailErrors: null` on a procedure is not enough when the service declares an interface | `push` | `drift + vailErrors, both levels` |
| 46. Rewriting a panel with identical HTML is not free, and it leaks listeners | `uilint` | `setHTML guard, see the console` |
| 47. The interface trap is worse than note 44 says: only execution proves it | `push` | `drift + smoke` |
| 13.3 The UI was pointed at the namespace it was imported from | `uilint` | `hardcoded-endpoint` |
| 13.9 Every approval decision in the UI was silently refused | `uilint` | `swallowed-refusal` |
| 13.19 The console was cutting off up to 40% of its own width, silently | `uilint` | `grid-min-width` |
| 13.22 Every em-dash in the agents was stored double-decoded | `lint + uilint` | `mojibake` |

## Convention only

These cannot be detected statically. They belong in the scaffold, in review, or in a comment next to the code they govern.

| Learning | What to do |
|---|---|
| `sort()` throws on an empty array | guard every sort and slice |
| `similaritySearch` positional signature, and the order of its results | positional signature |
| Retrieval needs a confidence floor, and so does keyword matching | retrieval needs a floor |
| Visibility must never depend on an animation | state, not animation |
| Threshold state survives a reset, so the second demo run behaves differently | reset must clear derived state |
| 24. Chart colours are computable, so compute them | validate contrast, do not eyeball |
| 25. `hidden` does nothing on a bare `<svg>` | svg needs display:none |
| 27. Read-then-insert dedup loses to concurrent agents | a unique index, not a read |
| 28. The UI felt slow because it re-asked, not because the server was slow | cache and diff before painting |
| 29. Concurrent mutations tore the store | serialise mutations |
| 37. A demo that stops itself, sized against the slowest heartbeat | watchdog sized to the slowest heartbeat |
| 38. Half the errors in the namespace were thrown by the error checker | do not let the checker be the top error |
| 45. A semantic index cannot be repointed; it has to be replaced | replace, never update |
| 13.13 The race that survived its own fix | a race that survives its own fix |
| 13.15 Activity replay reproduces `uuid()`, and the obvious repair was wrong | replay must not reproduce uuid() |
| 13.20 Days of supply went negative | a derived measure must not go negative |

## Enforced, from the pooled corpus

| id | Learning | Layer | Check |
|---|---|---|---|
| DM-01 | The source name in `SELECT ... FROM SOURCE` is a compile-time literal; a variable compiles a | `lint` | `dynamic-source` |
| DM-04 | VAIL `exception()` messages are Java MessageFormat; a bare `{}` placeholder raises "can't pa | `lint` | `exception-placeholder` |
| DM-05 | `Encode.base64()` is the base64 built-in that exists; four plausible spellings fail to compi | `lint` | `missing-builtin` |
| DM-06 | Creating a procedure over REST succeeds at the HTTP level even when it does not compile; the | `client` | `vail_errors, both levels` |
| DM-08 | With `hasExplicitInterface: true` the interface is a compiled contract that does not sync wi | `push` | `drift + vailErrors, both levels` |
| DM-16 | Instances of a custom type insert at `/resources/custom/<Type>`, not `/resources/types/<Type | `client` | `_path_trap, custom/ not types/` |
| DM-18 | Inserting into the procedures resource takes the VAIL under `script`, named `Service.operati | `client` | `procedures() reads `script`` |
| DM-21 | REST installs land in whatever namespace the auth token belongs to, with no failure signal - | `client` | `namespace() / assert_namespace` |
| NC-01 | PROCEDURE headers must use `Name(params): ReturnType` — no `END` terminator, and the return  | `lint` | `missing-return-colon + end-terminator` |
| NC-02 | `PUBLIC` (and presumably `PRIVATE`) as a procedure visibility modifier is a hard parse failu | `lint` | `public-modifier` |
| NC-04 | `Array` and `void` are not valid `PROCEDURE` return-type keywords — they get parsed as type  | `lint` | `bare-array-type` |
| NC-05 | A rule's `WHEN` clause needs an explicit `AS event` alias even for `MESSAGE ARRIVES FROM` —  | `lint` | `when-alias` |
| NC-06 | A Visual Event Handler's package/service name must be a compound (dotted) name — a plain sin | `client` | `body_trap` |
| NC-07 | `INSERT {...} INTO Type` (object-literal form) fails to parse; the working form is `INSERT T | `lint` | `insert-object-literal` |
| NC-08 | REST/MCP updates must strip server-managed fields before re-submitting a record — the platfo | `client` | `strip_server_fields` |
| NC-09 | Passing the offline `validateVAIL` lint check is not evidence the real compiler will accept  | `push` | `vailErrors is the gate, not validateVAIL` |
| NC-11 | Creating a `system.projects` resource with a `description` property fails outright | `client` | `body_trap` |
| NR-01 | Saving a procedure returns 200 even when it does not compile; errors sit in `vailErrors` unt | `client` | `vail_errors, both levels` |
| NR-02 | `validateVAIL` passes code that the real compiler rejects on deploy | `push` | `vailErrors is the gate, not validateVAIL` |
| NR-03 | Chaining a method call onto a procedure invocation is a compile error | `lint` | `chained-procedure-call` |
| NR-05 | `map` is an illegal parameter name | `lint` | `reserved-var` |
| NR-06 | A local variable named `match` with a method call is resolved as a procedure reference | `lint` | `reserved-var` |
| NR-07 | A parameter declared as bare `Array` is resolved as a missing custom type | `lint` | `bare-array-type` |
| NR-08 | `stateless` on a procedure that is not part of a service is a compile error | `lint` | `modifier-on-standalone` |
| NR-09 | `DateTime` is a raw `java.time.Instant` at runtime, so `toInteger(now())` and most convenien | `lint` | `instant-method` |
| NR-12 | `.contains()` inside a `WHERE` clause is bound as a procedure reference, not a String method | `lint` | `where-method-call` |
| NR-17 | An unqualified system service name inside a packaged file resolves into that package, and th | `lint` | `unimported-system-service` |
| NR-18 | An unqualified topic path in packaged VAIL is prefixed with the file's package, so publisher | `lint` | `unimported-topic` |
| NR-20 | A `PUBLISH ... TO TOPIC` from VAIL did not reach a WebSocket subscriber, while a REST publis | `uilint` | `topic-subscription` |
| NR-22 | Two `$` operator keys in one VAIL object literal collapse to the last key | `lint` | `dollar-operator-collapse` |
| NR-23 | A service with an explicit interface does not expose a new procedure until it is added to th | `push` | `drift + vailErrors, both levels` |
| NR-24 | An explicit service interface pins parameter names; renaming a procedure parameter breaks th | `push` | `drift + vailErrors, both levels` |
| NR-30 | System resource REST paths take no `system.` prefix; the error says `system.system.<name>` | `client` | `_path_trap, system. prefix` |
| NR-31 | For `genaiflows` and `collaborationtypes`, POST stores the body but PUT is what compiles it | `client` | `put(), POST then PUT compiles` |
| NR-33 | "Not found" is HTTP 400 with an error code, not 404 | `client` | `is_not_found` |
| NR-35 | The documented `/documents/<name>/content` endpoint returns 400 for an existing document; `/ | `client` | `document() uses /docs/` |
| NR-36 | PUT of a GET result fails on `_id` unless server-managed fields are stripped | `client` | `strip_server_fields` |
| NR-38 | REST create bodies for scheduled events, LLMs and rules differ from the obvious shape; `sour | `opscheck` | `scheduled_faults` |
| NR-48 | Re-saving everything listed by `/resources/procedures` creates namespace-local copies of sys | `client` | `procedures() filters ars_namespace` |
| NR-51 | A CLI profile's `namespace =` line can disagree with the namespace its token authenticates t | `client` | `namespace() / assert_namespace` |
| PS-02 | A `package` line in a topic-triggered rule corrupts the event-binding path | `lint` | `rule-topic-package` |
| PS-04 | No `dateDiff` / `dateAdd` builtin; a bare call is silently package-qualified and fails at st | `lint` | `instant-method + missing-builtin` |
| PS-09 | A Service with an explicit interface rejects any owned procedure that is not declared in tha | `push` | `drift + vailErrors, both levels` |
| PS-10 | Service interface says a param is `required: true` but the VAIL source omits the `Required`  | `push` | `drift + vailErrors, both levels` |
| PS-13 | A `private` service procedure rejects a direct external `execute` | `lint` | `private-cross-service` |
| PS-14 | A ScheduledEvent cannot publish to a `/services/<svc>/<event>` path | `opscheck` | `scheduled_faults` |
| PS-15 | A Service-Builder scheduled procedure's interval must be an even multiple of 60,000 ms | `opscheck` | `service_schedule_faults` |
| PS-16 | `createScheduledEvent` publishes a message to a topic — it cannot invoke a procedure | `opscheck` | `dead_schedules` |
| PS-22 | VAIL `PUBLISH ... TO TOPIC` does not reach WebSocket subscribers | `uilint` | `topic-subscription` |

## Convention only, from the pooled corpus

| id | Learning | What to do |
|---|---|---|
| DM-02 | A VAIL `SELECT count = count()` returns a one-row list, never a bare integer | a SELECT of count() is a one-row list |
| DM-10 | Service state variables on the deployed instance are plain typed properties set by direct as | confirm the version before using Concurrent.* |
| DM-11 | A scheduled (timed-event) procedure that throws does nothing, silently - and uninitialized s | initialise service state; wrap the tick in try/catch |
| DM-14 | The Python Connector wraps every script result in `pythonCallResults`; a procedure returning | unwrap pythonCallResults |
| DM-35 | A Vantiq deploy pushes the source project over the target and deletes whatever the target ha | a deploy deletes what the target has and the source does not |
| DM-36 | A connector script goes live as a Vantiq document - `WITH script = "x.py"` names the documen | the connector script is a document, not a file on the host |
| DM-37 | Client event subscriptions cannot be round-tripped through a project export - some artifacts | client event subscriptions are IDE-only |
| NC-03 | A multi-segment, package-qualified name in a `PROCEDURE` header can fail to parse even when  | retry a PROCEDURE header with the short service name |
| NC-10 | A subagent's listed MCP tool set is not proof those tools are actually callable in that invo | restart before trusting a subagent's MCP tool list |
| NR-13 | A bare procedure parameter inside `WHERE` is resolved against the queried type | alias a parameter before using it in WHERE |
| NR-14 | `return` inside a `catch` block does not stop the statements after the `try` | gate on a flag; return does not exit |
| NR-15 | `SELECT ONE` throws when more than one row matches; it is not "take the first" | SELECT ONE throws on 2+; use LIMIT = 1 |
| NR-26 | Querying a property marked `encrypted: true` returns zero rows with no error | an encrypted property cannot be queried at all |
| NR-27 | Reading `system.tokens` no longer returns `accessToken`; only the insert response has it | mint a token at the point of use |
| NR-41 | A Python connector reply sent from a background task races the connector's default echo and  | pass the correlation id in; do not reply |
| NR-42 | GenAI flow CodeBlock Python passes compile and fails at runtime under RestrictedPython | run a GenAI flow once before calling it done |
| NR-43 | The `genaiflows` payload uses `assembly`, `children[]`, `positions[taskKey]` and a `subassem | export a live instance; do not extrapolate |
| NR-49 | Credit quotas are per Manager; raising `credit.default` does not unblock GenAI 429s | quotas are per Manager |
| NR-50 | A misspelled quota key (`rates.streams`) is accepted and then breaks every later org read | copy quota keys from the default JSON exactly |
| PS-06 | `createProcedure`/`createRule` built-in lint: `single-quotes-invalid` and `var-scoping-in-if | the platform lint gate has known false positives |
| PS-12 | A rule cannot `PUBLISH` to a service OUTBOUND event; `PUBLISH ... TO SERVICE EVENT` is inbou | fan out on a Topic, not an outbound service event |
| PS-17 | Deleting/fetching a record by resource id requires the Mongo `_id`, not your `naturalKey` | address records by _id, not your natural key |
| PS-18 | This namespace has no data-generator Source implementation type | no data-generator sourceimpl; fake the feed |
| PS-20 | `mcp__vantiq-agentic__executeProcedure` with an empty `service` string silently creates a st | never pass an empty `service` to executeProcedure |
| PS-23 | A VAIL wrapper around a GenAI procedure defeats streaming | call the GenAI procedure directly or lose streaming |

## Not yet triaged

Recorded, but nobody has decided whether it is checkable. This list existing is the point: it shrinks or it explains itself.

- Platform behaviours found building this namespace
- Type definitions
- An unreachable branch in a severity ladder
- Math is available and exact
- Date and object idioms
- 21. Approval creation was not idempotent
- 22. "Slow to load" was the client, not the backend, and the first measurement lied
- 34. The trim generalised to one sibling demo, not seven
- 36. The cheap fix for Logistics would have broken its agents
- 39. The ping fix did not port. A demo that could not tick did.
- 41. Where the 61,000 actually lives, and which levers are real
- 13.1 Approval workflow never functioned
- 13.2 One of the twelve agents was completely dead
- 13.4 Approvals carried no risk basis
- 13.5 A content field was repurposed as a security field
- 13.6 `upsertIncident` threw on a finding with no decision
- 13.7 Escalation fed back into its own acknowledgment window (the <deployment> load incident)
- 13.8 The verification suite caught its own tamper tool lying
- 13.10 CCIR trip counters were unbounded
- 13.11 The second question to Mission Command overwrote the first answer
- 13.12 The error log, worked through end to end
- 13.14 The medical path was pointed at a unit that does not exist
- 13.16 The other half of the <deployment> load incident, found only after the audit store was cleared
- 13.17 A scenario that ran perfectly and demonstrated almost nothing
- 13.18 The audit replay guard needed a second layer
- 13.21 Two thirds of the collaboration record was one agent repeating itself
- 13.23 A collision test that skipped same-node pairs hid a collision
- 13.24 An expiry rendered with a function that measures the past
- DM-03 VAIL's lambda operator is `=>`, not `->`
- DM-07 "VAIL does not guarantee short-circuit evaluation of `//`" - asserted in shipped code, never watched to fa
- DM-09 `Context` propagates unchanged into a called service procedure
- DM-12 `Context.username()` is not a stable identifier - it returned a UUID on one check and the login name on a 
- DM-13 `system.users` has no active/enabled field - a Vantiq account cannot be deactivated from VAIL at all
- DM-15 The connector JSON-encodes every script global on completion and silently skips the unencodable ones at DE
- DM-17 Malformed JSON in a REST body gets a full Java stack trace back, with the exact line and column of the bad
- DM-19 The Python-connector SOURCE invocation key is `script` with a bare document name - not `code` with a path
- DM-20 Access tokens are namespace-scoped, not source-scoped - the `sources =` line in server.config routes a con
- DM-22 curl glob-expands `[` and `]`, so any Vantiq REST URL with `props=[...]` fails silently without `-g`
- DM-23 The two Vantiq MCP servers document different product generations, and neither necessarily matches the ins
- DM-24 The MCP `whoami` puts the bearer token into the transcript, and the MCP has no from-disk document upload
- DM-25 `readDocument` returns document content base64-encoded
- DM-26 Executing a procedure against an extension source with no connector attached fails with "not recognized as
- DM-27 A connector that fails authentication logs nothing but a repeating "is connecting" line - diagnose by cade
- DM-28 Deleting a token does not disturb an already-connected connector - and a dead token produces a container t
- DM-29 A connector with a bad credential crash-loops forever, silently - it does not park, back off, or alert
- DM-30 A stale connector session can block the new one from claiming the source - CONNECT_EXTENSION deactivation 
- DM-31 The connector's server.config is baked into the image by COPY - editing it and restarting changes nothing;
- DM-32 The Python connector leaks memory - a known Vantiq product defect; bouncing the container does not reclaim
- DM-33 Namespace invites are emailed by the installation's default EMAIL source, which you may not control - and 
- DM-34 Two connectors presenting the same source's credentials both attach, and Vantiq distributes calls across t
- DM-38 k8sdeploy_tools refuses to run unless targetCluster is a git repo with a fetchable origin, and `-Pcluster`
- DM-39 `configureVantiqSystem` recreates the targetCluster directory, destroying any git setup done before it
- DM-40 The `provider` value drives StorageClass creation; there is no `openshift` provider and `other` has no sto
- DM-41 Gradle injects `storageClass: vantiq-sc` into the helm values, and `encryptVolumes=false` does not stop it
- DM-42 The Vantiq chart's `mongo-available` init container uses an unqualified `mongo` image, which RHEL shortnam
- DM-43 Every `deployVantiq` (helm upgrade) silently reverts the init-container image patch
- DM-44 The Vantiq pod mounts a `dbbackup-creds` secret even when backup is disabled
- DM-45 The shared chart pins a bitnami mongodb-exporter tag that no longer exists on Docker Hub
- DM-46 Vantiq's OpenShift SCC needs are broader than `anyuid`, and pods admitted before a grant keep their old SC
- DM-47 The license is bound to an FQDN audience; changing the ingress domain to fix a derived URL crashes the ser
- DM-48 The k8sworker's bundled fabric8 client (6.7.1) cannot parse the Kubernetes 1.34 `/version` response and di
- DM-49 The k8sworker's `vantiq-worker` token secret is not created by the deploy
- DM-50 Vantiq 1.43.x will not start without an AI-assistant secret even when AI features are unused - and the log
- DM-51 There is no direct upgrade path 1.40.10 -> 1.43.17; the schema migrator is missing intermediate steps
- DM-52 Gradle reads a snapshot kubeconfig at `targetCluster/kubeconfig`, so an `oc login` refresh never reaches i
- DM-53 k8sdeploy_tools creates only Ingress resources, never OpenShift Routes; the InfluxDB databases are likewis
- DM-54 `generateSecrets` reports BUILD SUCCESSFUL even when secrets were not created or applied
- DM-55 Admin credentials on a fresh install live in MongoDB, not in anything the tooling hands you - and on 1.43 
- DM-56 A custom connector source type depends on a `sourceimpls` resource that is namespace-local and that subset
- DM-57 presetValues are delivered by text substitution into the script, so a preset key that is also an ordinary 
- DM-58 The connector executes exactly one substituted script; it cannot open or exec a sibling document, and the 
- DM-59 An unhandled Python exception in a connector script surfaces on the calling VAIL procedure as HTTP 400 car
- DM-60 A long-running connector script times out when invoked from a Vantiq procedure (~30 s observed)
- DM-61 The Vantiq mongo backup export can emit every document of a type roughly twice; raw row counts are ~2x the
- DM-62 A Vantiq complete backup contains TWO same-named JSON files per type - the type schema and the data - at p
- NR-04 A chained String call inside a compound `&&` condition compiles, then fails at runtime with a Groovy synta
- NR-10 A `$regex` literal inside `WITH where` throws a `GStringImpl` cast error at runtime, but a parameter value
- NR-11 `WITH where = {...}` accepts a raw query document, which is the only substring search VAIL has
- NR-16 `DELETE` with a filter that matches nothing throws `resource.not.found`
- NR-19 A topic event carries its payload in `event.value`; `event.newValue` is only on type events
- NR-21 `INSERT system.collaborations` from VAIL requires `results` and `collaborators`, and `UPDATE` needs `id` i
- NR-25 Creating a Visual Event Handler over REST takes POST, PUT, a service edit, and a second PUT before it leav
- NR-28 `expiresAt` on a `system.tokens` insert is not honoured
- NR-29 The `secrets` resource takes its value in a field named `secret`, and never returns it on read
- NR-32 POST to an existing `mcpservers` name returns 200 and changes nothing
- NR-34 A document's stored name comes from the multipart filename, not the `name` field
- NR-37 MCP server resources are exposed under a fixed `vantiq://` scheme, and the `mcpservers` REST shape is undo
- NR-39 PUT on an `llms` resource merges `config` instead of replacing it
- NR-40 An access token is bound to the namespace it was minted in and nothing re-scopes it
- NR-44 `Optional`, `Repeat` and `SemanticIndexWithCompression` throw a server NullPointerException when `subassem
- NR-45 Some GenAI component config properties are `unexposable` and cannot be targeted by Repeat, Assign or compo
- NR-46 A Visual Event Handler VAIL task needs config key `VAIL` and `useEventValue: true`, and `SaveToType` rejec
- NR-47 The typed `Procedure` component helper emits a shape the platform rejects
- NR-52 `Context.serverUri()` came back without a trailing slash on one deployment
- NR-53 A Deployment fails with `No such property: resourceType for class: java.lang.String` on every retry
- NR-54 Deleting a resource in the git export before deleting it in the namespace brings it back on the next expor
- PS-01 VAIL rule resource names cannot contain a dot
- PS-03 `random()` throws at runtime in this namespace and silently kills the enclosing handler
- PS-05 VAIL reserved words (`rule`, `state`) are rejected as variable/parameter names and lint does not flag it
- PS-07 `no sleep()`, `no array.remove(0)` in VAIL
- PS-08 A mid-procedure `return null` does not stop VAIL execution
- PS-11 An INBOUND service event type with no implementing resource is an ERROR, not a warning
- PS-19 `rulesSuppressed` defaults on for new types, which kills the type-CRUD live-update path
- PS-21 `mcp__vantiq-help__validateVAIL` is not available inside the Vantiq agentic MCP session
- PS-24 Turning on the GenAI-Agent (A2A) checkbox auto-creates a *separate* dispatch LLM and an empty secret
